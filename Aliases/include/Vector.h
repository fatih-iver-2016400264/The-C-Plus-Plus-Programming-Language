#ifndef VECTOR_H
#define VECTOR_H

template<typename T>
class Vector
{
    public:

        using value_type = T;

        Vector();
        virtual ~Vector();

    protected:

    private:
};

#endif // VECTOR_H
